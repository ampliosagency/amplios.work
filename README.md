# Amplios

Marketing website for Amplios, a marketing agency focused on creating landing pages and modern websites built for performance.

## Running locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Start the development server:
   `npm run dev`
3. Build the production version when needed:
   `npm run build`
